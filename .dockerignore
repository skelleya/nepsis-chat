# ── Node modules (Docker runs npm ci fresh) ──
node_modules/
**/node_modules/

# ── Electron — NOT needed for server (~11 GB of installers + win-unpacked) ──
electron/

# ── Frontend — NOT needed for backend (on Vercel) ──
frontend/

# ── Build artifacts ──
backend/data.sqlite

# ── Large binaries ──
**/*.exe
**/*.blockmap

# ── Git ──
.git/

# ── Env overrides ──
.env
.env.local

# ── Docs (not needed in image) ──
docs/

# ── Misc ──
*.md
scripts/
supabase/

# ── Keep backend source ──
!backend/
!backend/package*.json
!backend/src/
!backend/src/**
