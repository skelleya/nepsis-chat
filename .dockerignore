# ── Node modules (Docker runs npm ci fresh) ──
node_modules/
**/node_modules/

# ── Electron — NOT needed for server (~11 GB of installers + win-unpacked) ──
# Exclude explicitly (multiple patterns for compatibility)
electron
electron/
**/electron/**
electron/**

# ── Frontend — NOT needed for backend (on Vercel). Saves ~2 GB ──
frontend
frontend/

# ── Build artifacts ──
frontend/dist/
backend/data.sqlite

# ── Large binaries ──
**/*.exe
**/*.blockmap
# Allow installer in backend/updates for /updates/download (run "npm run release")
!backend/updates/*.exe

# ── Updates: run "npm run release" to populate before deploy ──
backend/updates/
!backend/updates/

# ── Git ──
.git/

# ── Env overrides ──
.env
.env.local

# ── Docs (not needed in image) ──
docs/

# ── Misc ──
*.md
scripts/
supabase/
