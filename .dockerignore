# ── Node modules (Docker runs npm ci fresh) ──
node_modules/
**/node_modules/

# ── Electron — NOT needed for server (~11 GB of installers + win-unpacked) ──
electron
electron/

# ── Build artifacts ──
frontend/dist/
backend/data.sqlite

# ── Large binaries ──
**/*.exe
**/*.blockmap
# Allow installer for download page (run "npm run package" + "copy-exe" before deploy)
!frontend/public/NepsisChat-Setup.exe
# Allow installer in backend/updates for /updates/download (run "npm run release")
!backend/updates/*.exe

# ── Updates: run "npm run release" to populate before deploy ──
backend/updates/
!backend/updates/

# ── Git ──
.git/

# ── Env overrides ──
.env
.env.local

# ── Docs (not needed in image) ──
docs/

# ── Misc ──
*.md
scripts/
supabase/
