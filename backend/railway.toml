# IMPORTANT: Railway service Root Directory must be `backend`
# No Dockerfile in this folder (see Dockerfile.vps for self-host) → Nixpacks + npm start.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
